apiVersion: v1
kind: Service
metadata:
  name: {{ include "parsers-chart.fullname" . }}
  labels:
    {{- include "parsers-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: parsers-selector
